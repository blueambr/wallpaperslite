# Wallpapers lite

## Prerequisites

- `bun install`

## Dev

- `bun dev`

## Build

- `bun run build`

## Preview

- `bun run build`
- `bun preview`

## Config

- Edit `svelte.config.js`, if another adapter is needed (default: Cloudflare)
- Edit `src/config.ts`
- Edit `src/data.ts`
